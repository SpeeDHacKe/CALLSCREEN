@inherits LayoutComponentBase

@using Microsoft.Extensions.Configuration;

@inject IConfiguration Configuration;

@if (isShowMenu)
{
    <div class="sidebar">
        <NavMenu />
    </div>
}

@*<div class="main">
            <div class="top-row px-4">
                <a href="https://docs.microsoft.com/aspnet/" target="_blank">About</a>
            </div>

            <div class="content px-4">
                @Body
            </div>
    </div>*@

<div class="main">
    <div class="content">
        @*@if (isShowMenu)
            {
                <div class="sidebar">
                    <NavMenu />
                </div>
            }*@

        @Body
    </div>
    @if (isShowFooter)
    {
        <footer class="modal-footer">
            <div class="container text-center">
                <span class="text-muted">Copyright © 2019 BIZKIT'S All Rights Reserved.</span>
            </div>
        </footer>
    }
</div>


<style>
    .footer {
        position: absolute;
        bottom: 0;
        width: 100%;
        height: 60px;
        line-height: 60px;
        background-color: #f5f5f5;
    }

    .modal-footer {
        background-color: #f5f5f5;
    }
</style>


@code {
    [Parameter] public bool isShowMenu { set; get; } = true;
    [Parameter] public bool isShowFooter { set; get; } = true;
    protected override async Task OnInitializedAsync()
    {
        //this.isShowMenu = (Configuration.GetValue(typeof(string), "Configuration:isShowMenu").ToString().ToLower() == "true" ? true : false);
        //this.isShowMenu = false;
        //this.isShowFooter = false;
    }
}
